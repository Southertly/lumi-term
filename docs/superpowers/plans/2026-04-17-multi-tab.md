# LumiTerm 多标签页 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 LumiTerm 添加多标签页支持，允许用户在单窗口中管理多个 PowerShell/CMD/WSL2 终端会话。

**Architecture:** 前端用 Pinia store 管理标签状态，每个标签对应独立的 xterm.js 实例和 PTY session。非活动标签通过 CSS `display:none` 隐藏，切换无延迟。后端 PTY service 无需修改（已支持多会话）。

**Tech Stack:** Vue 3, Pinia, xterm.js, Tauri Channel API, TypeScript

---

## 文件变更清单

| 操作 | 文件 | 说明 |
|------|------|------|
| 修改 | `src/stores/terminalStore.ts` | 扩展 store，添加 shellType、createTab、closeTab |
| 修改 | `src/App.vue` | 引入 TabBar 和 TerminalContainer |
| 新增 | `src/components/TabBar.vue` | 标签栏 + "+" 下拉菜单 |
| 修改 | `src/components/TerminalTab.vue` | 接收 tabId prop，支持多实例 |

---

### Task 1: 扩展 Pinia Store

**Files:**
- Modify: `src/stores/terminalStore.ts`

- [ ] **Step 1: 替换 store 内容**

```typescript
import { defineStore } from 'pinia';
import { ref } from 'vue';

export type ShellType = 'powershell' | 'cmd' | 'wsl2';

export interface Tab {
  id: string;
  title: string;
  shellType: ShellType;
  sessionId: string | null;
}

export const useTerminalStore = defineStore('terminal', () => {
  const tabs = ref<Tab[]>([]);
  const activeTabId = ref<string | null>(null);

  function createTab(shellType: ShellType = 'powershell') {
    const id = crypto.randomUUID();
    const titles: Record<ShellType, string> = {
      powershell: 'PowerShell',
      cmd: 'CMD',
      wsl2: 'WSL2',
    };
    tabs.value.push({ id, title: titles[shellType], shellType, sessionId: null });
    activeTabId.value = id;
    return id;
  }

  function setSessionId(tabId: string, sessionId: string) {
    const tab = tabs.value.find((t) => t.id === tabId);
    if (tab) tab.sessionId = sessionId;
  }

  function removeTab(id: string) {
    const index = tabs.value.findIndex((t) => t.id === id);
    if (index === -1) return;
    tabs.value.splice(index, 1);
    if (activeTabId.value === id) {
      activeTabId.value = tabs.value[index]?.id ?? tabs.value[index - 1]?.id ?? null;
    }
  }

  function switchTab(id: string) {
    if (tabs.value.some((t) => t.id === id)) {
      activeTabId.value = id;
    }
  }

  return { tabs, activeTabId, createTab, setSessionId, removeTab, switchTab };
});
```

- [ ] **Step 2: 验证 TypeScript 无报错**

运行：`npx tsc --noEmit`  
预期：无错误输出

- [ ] **Step 3: Commit**

```bash
git add src/stores/terminalStore.ts
git commit -m "feat: extend terminal store for multi-tab support"
```

---

### Task 2: 改造 TerminalTab.vue 支持多实例

**Files:**
- Modify: `src/components/TerminalTab.vue`

- [ ] **Step 1: 替换 TerminalTab.vue 内容**

```vue
<script setup lang="ts">
import { onMounted, onUnmounted, watch } from 'vue';
import { invoke } from '@tauri-apps/api/core';
import { Channel } from '@tauri-apps/api/core';
import { initTerminal, type TerminalInstance } from '../utils/xtermInitializer';
import { useTerminalStore, type ShellType } from '../stores/terminalStore';

const props = defineProps<{ tabId: string; shellType: ShellType; active: boolean }>();
const store = useTerminalStore();

let instance: TerminalInstance | null = null;
let resizeObserver: ResizeObserver | null = null;
let containerEl: HTMLElement | null = null;

const shellCommands: Record<ShellType, string> = {
  powershell: 'powershell.exe',
  cmd: 'cmd.exe',
  wsl2: 'wsl.exe',
};

async function init(container: HTMLElement) {
  containerEl = container;
  instance = initTerminal(container);
  const { terminal, fitAddon } = instance;

  const channel = new Channel<number[]>();
  channel.onmessage = (data) => terminal.write(new Uint8Array(data));

  try {
    const sessionId = await invoke<string>('create_pty', {
      shell: shellCommands[props.shellType],
      cols: terminal.cols,
      rows: terminal.rows,
      channel,
    });
    store.setSessionId(props.tabId, sessionId);
  } catch (e) {
    terminal.write(`\r\nFailed to start shell: ${e}\r\n`);
    return;
  }

  terminal.onData((data) => {
    const tab = store.tabs.find((t) => t.id === props.tabId);
    if (!tab?.sessionId) return;
    invoke('write_pty_cmd', {
      sessionId: tab.sessionId,
      data: Array.from(new TextEncoder().encode(data)),
    }).catch(() => {});
  });

  resizeObserver = new ResizeObserver(() => {
    fitAddon.fit();
    const tab = store.tabs.find((t) => t.id === props.tabId);
    if (tab?.sessionId) {
      invoke('resize_pty_cmd', {
        sessionId: tab.sessionId,
        cols: terminal.cols,
        rows: terminal.rows,
      }).catch(() => {});
    }
  });
  resizeObserver.observe(container);
}

watch(
  () => props.active,
  (active) => {
    if (active && instance) {
      setTimeout(() => instance!.fitAddon.fit(), 50);
    }
  }
);

onUnmounted(() => {
  resizeObserver?.disconnect();
  const tab = store.tabs.find((t) => t.id === props.tabId);
  if (tab?.sessionId) {
    invoke('close_pty_cmd', { sessionId: tab.sessionId }).catch(() => {});
  }
  instance?.dispose();
});
</script>

<template>
  <div
    ref="el"
    class="terminal-container"
    :style="{ display: active ? 'block' : 'none' }"
    @vue:mounted="(vnode: any) => init(vnode.el)"
  />
</template>

<style scoped>
.terminal-container {
  width: 100%;
  height: 100%;
  overflow: hidden;
  background: #1e1e2e;
}
</style>
```

- [ ] **Step 2: 验证 TypeScript 无报错**

运行：`npx tsc --noEmit`  
预期：无错误输出

- [ ] **Step 3: Commit**

```bash
git add src/components/TerminalTab.vue
git commit -m "feat: refactor TerminalTab to support multiple instances via props"
```

---

### Task 3: 新建 TabBar.vue

**Files:**
- Create: `src/components/TabBar.vue`

- [ ] **Step 1: 创建 TabBar.vue**

```vue
<script setup lang="ts">
import { ref } from 'vue';
import { useTerminalStore, type ShellType } from '../stores/terminalStore';

const store = useTerminalStore();
const dropdownOpen = ref(false);

const shells: { type: ShellType; label: string; icon: string; hint?: string }[] = [
  { type: 'powershell', label: 'PowerShell', icon: '❯', hint: 'Ctrl+T' },
  { type: 'cmd', label: 'CMD', icon: '⬛' },
  { type: 'wsl2', label: 'WSL2', icon: '🐧' },
];

function openTab(shellType: ShellType) {
  store.createTab(shellType);
  dropdownOpen.value = false;
}

function closeTab(e: MouseEvent, tabId: string) {
  e.stopPropagation();
  const tab = store.tabs.find((t) => t.id === tabId);
  if (!tab) return;
  if (confirm(`关闭 ${tab.title}？`)) {
    store.removeTab(tabId);
    if (store.tabs.length === 0) {
      // 关闭最后一个标签时退出应用
      import('@tauri-apps/api/core').then(({ invoke }) => invoke('close_app').catch(() => {}));
    }
  }
}
</script>

<template>
  <div class="tab-bar" @click.self="dropdownOpen = false">
    <div
      v-for="tab in store.tabs"
      :key="tab.id"
      class="tab"
      :class="{ active: tab.id === store.activeTabId }"
      @click="store.switchTab(tab.id)"
    >
      <span class="tab-icon">{{ tab.shellType === 'wsl2' ? '🐧' : '❯' }}</span>
      <span class="tab-title">{{ tab.title }}</span>
      <span class="tab-close" @click="closeTab($event, tab.id)">×</span>
    </div>

    <div class="new-tab-wrapper">
      <div
        class="new-tab-btn"
        :class="{ open: dropdownOpen }"
        @click.stop="dropdownOpen = !dropdownOpen"
      >+</div>
      <div v-if="dropdownOpen" class="dropdown" @click.stop>
        <div
          v-for="shell in shells"
          :key="shell.type"
          class="dropdown-item"
          @click="openTab(shell.type)"
        >
          <span class="item-icon">{{ shell.icon }}</span>
          <span class="item-label">{{ shell.label }}</span>
          <span v-if="shell.hint" class="item-hint">{{ shell.hint }}</span>
        </div>
      </div>
    </div>
  </div>
</template>

<style scoped>
.tab-bar {
  height: 40px;
  background: #181825;
  display: flex;
  align-items: center;
  padding: 0 8px;
  gap: 4px;
  border-bottom: 1px solid #11111b;
  overflow-x: auto;
  overflow-y: hidden;
}
.tab-bar::-webkit-scrollbar { height: 0; }

.tab {
  height: 32px;
  min-width: 140px;
  max-width: 200px;
  background: #1e1e2e;
  border: 1px solid #313244;
  border-radius: 6px;
  display: flex;
  align-items: center;
  padding: 0 10px;
  gap: 8px;
  cursor: pointer;
  transition: all 0.15s ease;
  flex-shrink: 0;
  color: #cdd6f4;
}
.tab:hover { background: #262637; border-color: #45475a; }
.tab.active { background: #89b4fa; border-color: #89b4fa; color: #11111b; }
.tab.active .tab-icon { color: #11111b; }
.tab.active .tab-close { color: #11111b; opacity: 0.6; }

.tab-icon { font-size: 13px; color: #89b4fa; flex-shrink: 0; }
.tab.active .tab-icon { color: #11111b; }
.tab-title { flex: 1; font-size: 13px; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; font-weight: 500; }
.tab-close {
  width: 16px; height: 16px;
  border-radius: 3px;
  display: flex; align-items: center; justify-content: center;
  font-size: 15px; opacity: 0;
  cursor: pointer;
  transition: all 0.15s ease;
}
.tab:hover .tab-close { opacity: 1; }
.tab-close:hover { background: rgba(108,112,134,0.2); color: #cdd6f4; }

.new-tab-wrapper { position: relative; }
.new-tab-btn {
  width: 32px; height: 32px;
  background: #1e1e2e;
  border: 1px solid #313244;
  border-radius: 6px;
  display: flex; align-items: center; justify-content: center;
  cursor: pointer;
  transition: all 0.15s ease;
  color: #89b4fa; font-size: 20px; line-height: 1;
  user-select: none;
}
.new-tab-btn:hover, .new-tab-btn.open { background: #262637; border-color: #45475a; }

.dropdown {
  position: absolute;
  top: 36px; left: 0;
  background: #181825;
  border: 1px solid #313244;
  border-radius: 8px;
  overflow: hidden;
  min-width: 160px;
  box-shadow: 0 8px 24px rgba(0,0,0,0.4);
  z-index: 100;
}
.dropdown-item {
  display: flex; align-items: center; gap: 10px;
  padding: 9px 14px;
  font-size: 13px; cursor: pointer;
  transition: background 0.1s ease;
  color: #cdd6f4;
}
.dropdown-item:hover { background: #313244; }
.item-icon { font-size: 14px; width: 18px; text-align: center; }
.item-label { flex: 1; }
.item-hint { font-size: 11px; color: #6c7086; }
</style>
```

- [ ] **Step 2: Commit**

```bash
git add src/components/TabBar.vue
git commit -m "feat: add TabBar component with shell type dropdown"
```

---

### Task 4: 更新 App.vue 整合所有组件

**Files:**
- Modify: `src/App.vue`

- [ ] **Step 1: 替换 App.vue 内容**

```vue
<script setup lang="ts">
import { onMounted, onUnmounted } from 'vue';
import TabBar from './components/TabBar.vue';
import TerminalTab from './components/TerminalTab.vue';
import { useTerminalStore } from './stores/terminalStore';

const store = useTerminalStore();

function handleKeydown(e: KeyboardEvent) {
  if (!e.ctrlKey) return;

  if (e.key === 't') {
    e.preventDefault();
    store.createTab('powershell');
  } else if (e.key === 'w') {
    e.preventDefault();
    if (store.activeTabId) {
      const tab = store.tabs.find((t) => t.id === store.activeTabId);
      if (tab && confirm(`关闭 ${tab.title}？`)) {
        store.removeTab(store.activeTabId);
      }
    }
  } else if (e.key === 'Tab') {
    e.preventDefault();
    const idx = store.tabs.findIndex((t) => t.id === store.activeTabId);
    if (store.tabs.length < 2) return;
    const next = e.shiftKey
      ? (idx - 1 + store.tabs.length) % store.tabs.length
      : (idx + 1) % store.tabs.length;
    store.switchTab(store.tabs[next].id);
  } else if (e.key >= '1' && e.key <= '9') {
    const idx = parseInt(e.key) - 1;
    if (store.tabs[idx]) {
      e.preventDefault();
      store.switchTab(store.tabs[idx].id);
    }
  }
}

onMounted(() => {
  store.createTab('powershell');
  window.addEventListener('keydown', handleKeydown);
});

onUnmounted(() => {
  window.removeEventListener('keydown', handleKeydown);
});
</script>

<template>
  <div class="app-container">
    <div class="titlebar" data-tauri-drag-region>
      <div class="titlebar-title">LumiTerm</div>
    </div>
    <TabBar />
    <div class="terminal-wrapper">
      <TerminalTab
        v-for="tab in store.tabs"
        :key="tab.id"
        :tab-id="tab.id"
        :shell-type="tab.shellType"
        :active="tab.id === store.activeTabId"
      />
    </div>
  </div>
</template>

<style>
* { margin: 0; padding: 0; box-sizing: border-box; }
html, body, #app { width: 100%; height: 100%; overflow: hidden; }
.app-container {
  width: 100%; height: 100%;
  display: flex; flex-direction: column;
  background: #1e1e2e;
}
.titlebar {
  height: 32px; background: #181825;
  display: flex; align-items: center;
  padding: 0 12px; user-select: none;
}
.titlebar-title { font-size: 13px; color: #cdd6f4; font-weight: 500; }
.terminal-wrapper { flex: 1; overflow: hidden; position: relative; }
</style>
```

- [ ] **Step 2: 验证 TypeScript 无报错**

运行：`npx tsc --noEmit`  
预期：无错误输出

- [ ] **Step 3: Commit**

```bash
git add src/App.vue
git commit -m "feat: integrate TabBar and multi-tab TerminalTab in App.vue"
```

---

### Task 5: 添加 close_app Tauri 命令（关闭最后标签时退出）

**Files:**
- Modify: `src-tauri/src/commands/pty.rs`
- Modify: `src-tauri/src/lib.rs`

- [ ] **Step 1: 在 pty.rs 末尾添加 close_app 命令**

在 `src-tauri/src/commands/pty.rs` 末尾添加：

```rust
#[tauri::command]
pub fn close_app(app: tauri::AppHandle) {
    app.exit(0);
}
```

- [ ] **Step 2: 在 lib.rs 注册命令**

找到 `invoke_handler` 中的命令列表，添加 `close_app`：

```rust
.invoke_handler(tauri::generate_handler![
    pty::create_pty,
    pty::write_pty_cmd,
    pty::resize_pty_cmd,
    pty::close_pty_cmd,
    pty::close_app,
])
```

- [ ] **Step 3: 验证 Rust 编译**

运行：`cargo check --manifest-path=src-tauri/Cargo.toml`  
预期：`Finished` 无错误

- [ ] **Step 4: Commit**

```bash
git add src-tauri/src/commands/pty.rs src-tauri/src/lib.rs
git commit -m "feat: add close_app command for graceful exit on last tab close"
```

---

### Task 6: 手动测试验证

- [ ] **Step 1: 启动开发模式**

在终端运行（需手动执行，长时间运行进程）：
```bash
npx pnpm tauri dev
```

- [ ] **Step 2: 测试基础功能**

1. 应用启动后自动打开一个 PowerShell 标签
2. 点击 "+" 按钮，验证下拉菜单显示 PowerShell / CMD / WSL2
3. 选择 CMD，验证新标签打开并可以输入命令
4. 点击标签切换，验证两个终端状态独立保持
5. 点击 × 关闭标签，验证进程终止

- [ ] **Step 3: 测试快捷键**

1. Ctrl+T：新建 PowerShell 标签
2. Ctrl+W：关闭当前标签（有确认弹窗）
3. Ctrl+Tab / Ctrl+Shift+Tab：循环切换标签
4. Ctrl+1 / Ctrl+2：跳转到指定标签

- [ ] **Step 4: 测试边界情况**

1. 关闭最后一个标签，验证应用退出
2. 打开 5+ 个标签，验证标签栏可横向滚动
3. 窗口 resize，验证所有标签的终端正确适配

---

## 自审检查

- [x] **Spec 覆盖：** 所有功能点均有对应 Task
- [x] **无占位符：** 所有步骤包含完整代码
- [x] **类型一致性：** `ShellType`、`Tab`、`createTab`、`removeTab`、`switchTab` 在所有 Task 中命名一致
- [x] **文件路径：** 所有路径均为精确路径
