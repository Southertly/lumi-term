# Workspace Sidebar 修复测试清单

**测试日期**: 2026-05-06  
**修复版本**: 所有 P0 和 P1 问题已修复  
**测试目的**: 验证所有修复功能正常工作，没有引入新问题

---

## ✅ 测试前准备

1. 启动 Tauri 应用：`npm run tauri dev`
2. 确保应用完全加载
3. 打开开发者工具（F12）查看控制台错误

---

## 🔴 P0 - 关键问题测试

### 1. 路径遍历攻击防护测试

**测试目标**: 验证文件名验证函数防止路径遍历攻击

**测试步骤**:
1. 在文件树中右键点击任意文件夹
2. 选择"新建文件"
3. 尝试输入以下文件名（应该全部被拒绝）:
   - [ ] `../../../etc/passwd`
   - [ ] `..`
   - [ ] `.`
   - [ ] `test/file.txt` (包含 `/`)
   - [ ] `test\file.txt` (包含 `\`)
   - [ ] 空文件名
   - [ ] `CON` (Windows 保留名称，如果在 Windows 上)

**预期结果**: 所有非法文件名都应该显示 Toast 错误提示，文件不会被创建

**实际结果**: ___________

---

### 2. 并发打开文件竞态条件测试

**测试目标**: 验证快速双击文件不会打开两次

**测试步骤**:
1. 在文件树中找到一个文本文件
2. 快速双击该文件 3-5 次
3. 检查编辑器标签页数量

**预期结果**: 只打开一个标签页，不会出现重复的标签页

**实际结果**: ___________

---

### 3. 内存泄漏防护验证

**测试目标**: 验证 pendingOpens Map 正确清理

**测试步骤**:
1. 打开开发者工具控制台
2. 快速打开和关闭 10 个不同的文件
3. 在控制台中检查是否有内存泄漏警告

**预期结果**: 没有内存泄漏警告，应用运行流畅

**实际结果**: ___________

---

## 🟡 P1 - 重要问题测试

### 4. 文件树递归展开节流测试

**测试目标**: 验证快速点击文件夹不会发起重复请求

**测试步骤**:
1. 打开开发者工具的 Network 标签
2. 找到一个包含子文件夹的文件夹
3. 快速点击该文件夹 5-10 次（展开/折叠）
4. 检查 Network 标签中的 `list_directory` 请求数量

**预期结果**: 每个文件夹只发起一次 `list_directory` 请求，不会有重复请求

**实际结果**: ___________

---

### 5. Toast 通知测试

**测试目标**: 验证 Toast 通知正常显示和自动关闭

**测试步骤**:
1. 在文件树中右键点击文件
2. 选择"删除"
3. 取消删除操作
4. 尝试创建一个已存在的文件（触发错误 Toast）
5. 观察 Toast 通知

**预期结果**: 
- Toast 从右上角滑入
- 显示红色错误样式
- 5 秒后自动消失
- 点击 Toast 可以立即关闭

**实际结果**: ___________

---

### 6. ConfirmDialog 测试

**测试目标**: 验证确认对话框正常工作

**测试步骤**:
1. 打开一个文件并修改内容（不保存）
2. 尝试关闭该文件标签页
3. 观察确认对话框

**预期结果**:
- 显示确认对话框，标题为"关闭文件"
- 消息为"关闭 xxx？未保存的更改会丢失。"
- 有"取消"和"确定"两个按钮
- 按 Enter 键确认关闭
- 按 Escape 键取消
- 点击背景遮罩取消

**实际结果**: ___________

---

### 7. 键盘导航测试

**测试目标**: 验证工作区浏览器的键盘导航

**测试步骤**:
1. 点击侧边栏顶部的工作区路径（打开工作区浏览器）
2. 使用键盘操作:
   - [ ] 按 ↓ 键向下移动选择
   - [ ] 按 ↑ 键向上移动选择
   - [ ] 按 Enter 键选择工作区
   - [ ] 按 Escape 键关闭浏览器
   - [ ] 按 Tab 键切换焦点

**预期结果**: 所有键盘操作都正常工作

**实际结果**: ___________

---

### 8. 工作区切换时处理编辑器测试

**测试目标**: 验证切换工作区时提示关闭文件

**测试步骤**:
1. 打开 2-3 个文件
2. 点击侧边栏顶部的工作区路径
3. 选择另一个工作区
4. 观察确认对话框

**预期结果**:
- 显示确认对话框，标题为"切换工作区"
- 消息为"切换工作区将关闭所有打开的文件。是否继续？"
- 点击"取消"，工作区不切换，文件仍然打开
- 点击"确定"，工作区切换，所有文件关闭

**实际结果**: ___________

---

## 🔧 代码审查发现的问题测试

### 9. closeFile force 参数测试

**测试目标**: 验证 force 参数正确工作

**测试步骤**:
1. 打开一个文件并修改内容（不保存）
2. 切换工作区
3. 在确认对话框中点击"确定"

**预期结果**: 
- 只显示一次确认对话框（工作区切换确认）
- 不会为每个文件单独显示确认对话框
- 所有文件被强制关闭

**实际结果**: ___________

---

## 🎯 综合测试场景

### 10. 完整工作流测试

**测试步骤**:
1. 创建一个新文件夹
2. 在文件夹中创建一个新文件
3. 双击打开该文件
4. 编辑文件内容
5. 保存文件
6. 重命名文件
7. 删除文件
8. 观察整个过程中的 Toast 通知和确认对话框

**预期结果**: 所有操作流畅，Toast 和对话框正常显示

**实际结果**: ___________

---

## 📊 测试结果总结

**通过的测试**: _____ / 10  
**失败的测试**: _____  
**发现的新问题**: _____

### 发现的问题列表

1. ___________
2. ___________
3. ___________

---

## 🚀 下一步行动

- [ ] 修复发现的问题
- [ ] 重新测试失败的场景
- [ ] 推送代码到远程仓库
- [ ] 创建 Pull Request

---

**测试人员**: ___________  
**测试完成时间**: ___________
